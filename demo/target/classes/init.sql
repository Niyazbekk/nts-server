CREATE TABLE t_user
(
    id       BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    username VARCHAR(255),
    password VARCHAR(255),
    CONSTRAINT pk_t_user PRIMARY KEY (id)
);
CREATE TABLE t_role
(
    id   BIGINT NOT NULL,
    name VARCHAR(255),
    CONSTRAINT pk_t_role PRIMARY KEY (id)
);

CREATE TABLE t_user_roles
(
    user_model_id BIGINT NOT NULL,
    roles_id      BIGINT NOT NULL,
    CONSTRAINT pk_t_user_roles PRIMARY KEY (user_model_id, roles_id)
);

ALTER TABLE t_user_roles
    ADD CONSTRAINT fk_tuserol_on_role FOREIGN KEY (roles_id) REFERENCES t_role (id);

ALTER TABLE t_user_roles
    ADD CONSTRAINT fk_tuserol_on_user_model FOREIGN KEY (user_model_id) REFERENCES t_user (id);
